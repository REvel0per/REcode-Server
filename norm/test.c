#include <stdio.h>  int* foo(); int* list_r();  int main() { 	//Resource Leak 	FILE *pFile = fopen("Test.c", "r");  	//Dead Store 	int i = 1;  	//Memory Leak 	int *list = malloc(8);  	//Null Dereference 	int *p = 0; 	*p = 1;   	//Stack Variable Address Escape 	foo();   	//Use After Free 	list_r();  	//condition always false 	int f = 0; 	if(f){ 	  return 0; 	}  	//Integer Overflow 	int k = 2147483648; 	k = k + 5;  	//Buffer Overrun 	int l[5] = {1, 2, 3, 4, 5}; 	l[6];  	//Uninitailized Value 	int j; 	return j; }  int* foo() {   int x = 4;   return &x; }  int* list_r() { 	//Use After Free 	int *list = malloc(8); 	*list = 1; 	free(list); }